apiVersion: otel.splunk.com/v1alpha1
kind: Agent
metadata:
  name: test-default
spec:
  clusterName: test-cluster
  realm: my-splunk-realm
  agent:
    resources:
      limits:
        cpu: 200m
        memory: 200Mi
      requests:
        memory: 100Mi
        cpu: 100m